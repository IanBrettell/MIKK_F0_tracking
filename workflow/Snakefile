include: "rules/00_common.smk"

##### Target rules #####

rule all:
    input:
#####################
# 01_pre-processing
#####################
# copy_videos
        expand(os.path.join(config["working_dir"], "raw_videos/{sample}.avi"),
                sample = SAMPLES
        ),
# set_split_coords
        expand("results/split_coord_images/{sample}.png",
                sample = SAMPLES
        ),
# split_videos
        expand(os.path.join(config["data_store_dir"], "split/{assay}/{sample}_{quadrant}.mp4"),
                assay = ASSAYS,
                sample = SAMPLES,
                quadrant = QUADRANTS
        ),

#####################

##### Modules #####

include: "rules/01_pre-processing.smk"
